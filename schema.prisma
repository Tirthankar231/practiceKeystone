// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource postgresql {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        String    @id @default(uuid()) @postgresql.Uuid
  name      String    @default("")
  email     String    @unique @default("")
  password  String
  role      String?   @default("user")
  createdAt DateTime? @default(now())
}

model Order {
  id                  String              @id @default(uuid()) @postgresql.Uuid
  orderId             String              @default("")
  currency            String              @default("")
  value               String              @default("")
  bff                 String              @default("")
  collectedBy         String              @default("")
  paymentType         String              @default("")
  createdAt           DateTime?           @default(now())
  state               String              @default("")
  updatedAt           DateTime?           @default(now())
  seller              Seller?             @relation("Order_seller", fields: [sellerId], references: [id])
  sellerId            String?             @map("seller") @postgresql.Uuid
  settlementDetails   Settlement_Details? @relation("Order_settlementDetails", fields: [settlementDetailsId], references: [id])
  settlementDetailsId String?             @unique @map("settlementDetails") @postgresql.Uuid
  relatedLinks        Json?

  @@index([sellerId])
}

model Seller {
  id     String  @id @default(uuid()) @postgresql.Uuid
  gst    String  @default("")
  pan    String  @default("")
  bpp_id String  @default("")
  name   String  @default("")
  orders Order[] @relation("Order_seller")
}

model Settlement_Details {
  id             String @id @default(uuid()) @postgresql.Uuid
  settlementType String @default("")
  accountNo      String @default("")
  bankName       String @default("")
  branchName     String @default("")
  order          Order? @relation("Order_settlementDetails")
}
